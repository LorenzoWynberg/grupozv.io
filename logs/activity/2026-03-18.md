# Activity Log — 2026-03-18

## SEO Optimization

**Time:** ~10:00 CST (UTC-6)

### Changes Made

#### New Files

- **`lib/seo.ts`** — Shared helper for generating canonical URLs and hreflang alternates
- **`app/robots.ts`** — robots.txt with sitemap reference, allows all crawling except /api/
- **`app/sitemap.ts`** — XML sitemap with i18n alternates for all 12 routes (6 static + 6 brand pages)
- **`components/seo/json-ld.tsx`** — Organization and WebSite structured data (Schema.org JSON-LD)

#### Modified Files

- **`app/layout.tsx`** — Added `lang` attribute on `<html>` from middleware `x-locale` header
- **`app/[locale]/layout.tsx`** — Full metadata: metadataBase, title template, keywords, authors, OG tags, Twitter cards, robots directives, formatDetection, generateStaticParams, JSON-LD components
- **`app/[locale]/page.tsx`** — Added generateMetadata with alternates, hero image `priority` + `sizes` props
- **`app/[locale]/about/page.tsx`** — Added alternates to metadata
- **`app/[locale]/calendar/page.tsx`** — Added alternates to metadata
- **`app/[locale]/invest/page.tsx`** — Added alternates to metadata
- **`app/[locale]/team/page.tsx`** — Added alternates to metadata
- **`app/[locale]/portfolio/page.tsx`** — Added alternates to metadata
- **`app/[locale]/portfolio/[slug]/page.tsx`** — Added alternates to metadata + gallery image sizes
- **`next.config.ts`** — Enabled AVIF image format alongside WebP

#### Verification

- `pnpm lint` — passed
- `pnpm format` — passed
- `pnpm build` — passed (robots.txt and sitemap.xml generated as static routes)
